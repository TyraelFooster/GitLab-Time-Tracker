# GitLab Time Tracking Dashboard

A comprehensive Next.js dashboard for visualizing GitLab project time tracking, commit activity, and team collaboration.

## ✨ Features

- **Dynamic Time Tracking**: Real-time data from GitLab timelogs and issues
- **Epic-based Analytics**: Weekly breakdown by epic with interactive charts
- **Commit Activity Heatmap**: GitHub-style commit visualization with hover details
- **Team Collaboration**: Automatic contributor discovery from GitLab
- **Interactive Charts**: SVG-based charts with hover tooltips and filtering

## 🚀 Quick Start

1. **Clone the repository**

   ```bash
   git clone https://github.com/NilDitt/GitLab-Time-Tracker.git
   cd Random
   ```

2. **Install dependencies**

   ```bash
   npm install
   ```

3. **Configure GitLab access**

   - Edit `app/config/tracker-config.ts`
   - Update `GITLAB_CONFIG` with your GitLab project details:
     ```typescript
     export const GITLAB_CONFIG = {
       API_URL: "https://gitlab.com/api/graphql",
       PROJECT_PATH: "your-group/your-project", // e.g. "dhbw-se/se-tinf24b2/gruppe-4/dhubbw"
       TOKEN: "your-gitlab-token", // you need to give read API Access
     } as const;
     ```

4. **Start development server**

   ```bash
   npm run dev
   ```

5. **Open in browser**
   - Navigate to [http://localhost:3001](http://localhost:3001)

## 📋 Prerequisites

- **Node.js** 18+
- **GitLab Access Token** with API permissions
- **GitLab Project** with time tracking enabled

## 🛠️ Technology Stack

- **Framework**: Next.js 14.2.3 with TypeScript
- **Charts**: Custom SVG-based visualization components
- **API**: GitLab GraphQL and REST APIs
- **Styling**: CSS-in-JS with TypeScript

## 📊 Chart Components

- **AreaChart**: Time trends over weeks
- **BarChart**: Category and team comparisons
- **CommitActivityChart**: Heatmap with commit details on hover
- **DonutChart**: Category distribution
- **WeeklyStackedBarChart**: Epic breakdown by team member

## 🔧 Configuration

### GitLab Setup

1. Create a personal access token in GitLab
2. Ensure the token has `api` scope
3. Update the configuration in `tracker-config.ts`

### Time Range

The dashboard automatically fetches data from the last 12 weeks. Modify the date range in `TimeTrackerDashboard.tsx` if needed.

## 📁 Project Structure

```
app/
├── components/          # Chart and UI components
├── config/             # GitLab and app configuration
├── lib/                # GitLab API integration
├── api/                # Next.js API routes
├── globals.css         # Global styles
├── layout.tsx          # App layout
└── page.tsx           # Main dashboard page

package.json           # Dependencies and scripts
tsconfig.json         # TypeScript configuration
next.config.mjs       # Next.js configuration
.gitignore           # Git exclusions
```

## 🎯 Key Features Explained

### Commit Hover Tooltips

- Hover over any commit activity tile to see detailed commit information
- Shows commit titles, authors, and clickable GitLab links
- Supports days with multiple commits

### Epic-based Time Tracking

- Automatically aggregates time by epic from GitLab issues
- Interactive filtering by epic
- Color-coded visualization

### Dynamic Contributors

- Automatically discovers team members from GitLab timelogs
- No need to hardcode team member lists

## 🔄 Development

### Build for Production

```bash
npm run build
npm start
```

### TypeScript Checking

```bash
npm run type-check
```

## 📝 Notes

- `.next/` and `node_modules/` are auto-generated and excluded from git
- `next-env.d.ts` is auto-generated by Next.js
- The dashboard fetches data client-side from GitLab APIs
- Sensitive tokens should be managed via environment variables in production

## 🤝 Contributing

1. Fork the repository
2. Create a feature branch
3. Make your changes
4. Test thoroughly
5. Submit a pull request
